Resonance — terminal music player.

## Build & run

```bash
cd cmd/resonance
go build -o resonance .
./resonance
```

On first launch, choose your music library path (or press `1` to use `~/Music`).

## HotKeys

`↑↓`/`jk` navigate · `Enter` open dir or play · `Backspace`/`h` parent dir · `a` add to queue · `A` add all · `Space` pause/resume · `n`/`p` skip · `[`/`]` volume · `m` mute · `Tab`/`←→` switch panel · `q` quit
